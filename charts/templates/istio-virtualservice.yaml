{{- if and .Values.istio .Values.istio.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "support-storage-service.fullname" . }}
  labels:
    {{- include "support-storage-service.labels" . | nindent 4 }}
spec:
  hosts:
    - {{ include "support-storage-service.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local
  http:
    - match:
        - uri:
            prefix: /
      route:
        - destination:
            host: {{ include "support-storage-service.fullname" . }}
            port:
              number: {{ .Values.service.port }}
{{- end }}