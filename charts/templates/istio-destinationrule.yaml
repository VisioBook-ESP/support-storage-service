{{- if and .Values.istio.enabled .Values.istio.destinationRule.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: {{ include "support-storage-service.fullname" . }}
  labels:
    {{- include "support-storage-service.labels" . | nindent 4 }}
spec:
  host: {{ include "support-storage-service.fullname" . }}
  trafficPolicy:
    {{- toYaml .Values.istio.destinationRule.trafficPolicy | nindent 4 }}
    {{- if .Values.istio.mtls.enabled }}
    tls:
      mode: {{ .Values.istio.mtls.mode }}
    {{- end }}
{{- end }}